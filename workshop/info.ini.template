; info.ini 的源模板。真正的 info.ini 是构建产物，不纳入版本控制。
;
; 为什么要有模板：版本号只允许有一个来源，即 EliteEnemies.csproj 的 <Version>。
; 构建时把下面那行 @VERSION@ 替换为实际版本号，生成 info.ini 再部署。
; 于是「改了 csproj 忘了改 info.ini」这件事物理上不可能发生。
;
; 本文件里以分号开头的行都是注释，构建时会从生成的 info.ini 中剔除
; （游戏侧的解析器把任何非空、非 [ 开头的行都按 key=value 读，
;   注释行一旦含等号就会被当成一个多余的键，所以生成时要滤掉）。
;
; 游戏侧依据：ModManager.cs:304 读 name、:309 读 displayName、:314 读 description，
; 三者缺任一个模组都不会出现在游戏列表里；且 name 还同时决定 DLL 文件名
; （ModInfo.cs:30）与入口类型全名（ModManager.cs:445）。

name = EliteEnemies
displayName = 精英怪 / EliteEnemies
description = 随机生成带词条的精英怪 by LOVEIII486
version = @VERSION@
; 工坊分类标签。**逗号分隔**（游戏侧 SteamWorkshopManager 用 tags.Split(",") 切，
; 每段再 Trim）。取值应取自工坊筛选栏开放的那些，写别的 Steam 会收但筛选栏点不到。
;
; ★ **这一行必须非空**，否则标签会被清掉——实测过的机制：
;   ModManager.cs:291/295 的解析是 `if (num >= 1 && num + 1 < text.Length)`，
;   **值部分为空的行会被整行跳过**，于是 tags 这个键根本不存在，
;   取默认值 ""，而设置标签那段是 `if (!string.IsNullOrWhiteSpace(tags))`，
;   直接跳过 → 上传后条目只剩 Steam 补的 ["Mod"]。
;   （作者的分类标签就是这么没的：模板里那一行原本是空的 `tags =`。）
;
; ⚠ 还有一个**未解的矛盾**，下次上传时正好一并验掉：
;   SteamWorkshopManager.cs:225 在按本行设完之后，**又无条件 SetItemTags(["Mod"])**
;   一次（已反编译安装的那份 TeamSoda.Duckov.Core.dll 确认，不是过期的源码树）。
;   两次调用作用在同一个 handle 上，按理后写的赢——但抽样 20 个同游戏模组，
;   18 个都带着真实分类标签，说明标签确实留得住。
;   ⇒ 上传后用 GetPublishedFileDetails 查 tags 即可定论（2026-09-19 实测：标签留得住）。
;   （曾为此写过一个 F8 手动工具，**已删除**——不需要手动做，别再把它写回来。）
tags = Gameplay,Companion & NPC,Loot & Economy,Multiplayer & Co-op
publishedFileId = 3602009885
