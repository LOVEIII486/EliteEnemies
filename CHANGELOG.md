# 更新说明

> **这份文件做什么**：① 记下每次发版的对外说明；② 说明这些说明该怎么写；③ 版本号怎么涨；④ 发版按哪几步做。
>
> 它**不随模组分发、也不上创意工坊页**——只放在仓库里，发版时从这里取 BBCode 贴出去。
> 内容和写作标准放在同一份文件里，是因为两者必须一起改：标准一变，写法就跟着变。

---

## 一、版本号怎么涨

| 涨哪一位 | 什么时候涨 | 例子 |
|---|---|---|
| 第三位 · 修订 `2.0.0 → 2.0.1` | 修 bug、调数值、改文案。玩家感觉是"同一个版本变好了"，**没有新东西** | 修好某个词条的报错 |
| 第二位 · 次版本 `2.0.0 → 2.1.0` | 加了新词条 / 新功能 / 新设置项，或者**重做**了现有玩法。玩家需要重新了解 | 严寒词条重做、音乐家会吹曲子 |
| 第一位 · 主版本 `2.0.0 → 3.0.0` | 大改版：整体重做，或老存档 / 老设置不再兼容 | — |

**⚠ 修订号只涨到 9。** 到了 `2.0.9` 还要再发一个修订版，就进 `2.1.0`——不要出现 `2.0.10`。
两位数的修订号不好读；而且真到了那一步，多半意味着这堆小修小补已经够算一个版本了。

> 涨完版本号记得同步 `EliteEnemies.csproj` 的 `<Version>`：构建会强制校验它与
> `info.ini` 相等，不一致**直接编译失败**（这是故意的，防止两处版本号漂移）。

**每次发版都在这份文件里记一节**（哪怕是修订版——玩家也想知道修了什么）；
但只有**次版本或主版本**（也就是"大更新"）才值得在创意工坊单独发一条公告。

---

## 二、更新说明怎么写

### 三条硬规则

1. **写玩家能感觉到的变化，不写实现。**

   | ✅ 这么写 | ❌ 别这么写 |
   |---|---|
   | 语言切换即时生效 | 本地化改为按版本号失效的缓存 |
   | 掉落的装备品质不再溢出 | 修掉了品质档位与倍率的消饱和问题 |

2. **不写这些词**：类名、文件名、提交号、内部术语（反射 / 缓存 / 重构 / API / 补丁 / 参数名），
   以及「共 N 处」这类数字——玩家看不懂，而且数字一定会腐烂。

3. **拿不准就假设读者从没玩过这个模组。** 术语换成大白话，缩写先展开。

### 格式

固定四个分类，**有内容才写那一节**，没有就整节不写（不要写"无变化"）：

| 分类 | 放什么 |
|---|---|
| `✨ 新增` | 新词条、新功能、新设置项 |
| `⚖️ 平衡` | 数值调整、行为改了、难度变了 |
| `🐛 修复` | 之前是坏的、现在正常了 |
| `⚙️ 优化` | 体验更顺、占用更低、界面更好用 |

模板（直接照抄，只改版本号和内容）：

```
[h1]v2.0.0 更新内容[/h1]

[b]⚙️ 优化[/b]
- 语言切换即时生效
- 掉落系统重做
- 设置界面重整
- 各处系统开销优化，降低游戏占用
```

### 两条最容易漏的

- **破坏性变更单独列一条**，并说清楚会怎样（老存档 / 老设置要不要重新配）。
  别混在「优化」里——玩家按老习惯玩会踩坑，而这类问题事后才反馈的成本最高。
- **同一次发版内的多个提交合并成一条**。比如某个功能中间改了四五稿，对玩家来说只是
  「这个功能做好了」，不该出现四行。

### 写之前做什么

把上一个版本之后的提交过一遍（`git log`），逐条判断**玩家看不看得见**。
看不见的直接不进这份文档——内部重构、诊断日志、注释修正、变量改名都属于这类。
判断标准就是第一条规则：说得出"玩家会感觉到什么"才写，说不出来就不写。

---

## 三、发版步骤

按顺序做。**关键约束：构建必须在提交之后**——DLL 会内嵌提交 SHA
（`<Version>+<commit SHA>`，**换提交必变**）。
先构建后提交的话，部署目录里那份挂的是上一个提交的 SHA，
"部署的到底是哪一版"就说不清了。这一条真踩过。

> ⚠ 顺带更正一句曾经写在这里的话：**"同一提交重编逐字节一致"是错的**。
> 实测把 tag 签出到另一个目录（`git worktree`）再构建，内嵌 SHA 相同、
> **文件哈希不同**（确定性构建把源码路径算进了 MVID）。所以发布产物**不能靠重建还原**——
> 但也正因为本工程不公布文件哈希，这件事没有实际影响。

1. **涨版本号**：改 `EliteEnemies.csproj` 的 `<Version>`（规则见第一节）。
   这是全工程**唯一**的版本来源——`info.ini` 由构建期从模板生成，
   且会强制校验与它相等，不一致**直接编译失败**。

2. **先把生成物跑一遍**：
   ```bash
   py tools/gen-affix-overview.py
   ```
   `workshop\affix-overview.bbcode.txt` 是**提交进仓库的生成物**，标题里带版本号。
   不先跑这一遍，第 5 步的构建就会把它改掉，于是**提交完版本号、工作区反而是脏的**，
   而那个改动应当属于这次发版。（实测踩到：打完 v2.2.0 才发现生成物还写着 v2.1.0。）

3. **定稿更新说明**：把第四节的对应小节从"草稿 · 未发布"改成"已发布"。写法见第二节。

4. **提交**。到这里工作区应当是干净的。

5. **构建并部署**：
   ```bash
   dotnet build -c Release -t:Rebuild
   ```
   必须带 `-t:Rebuild`：源码没变时增量构建会打印"0 个警告"而不重编译，那是假结果。

6. **核对三样**（`MOD` 指游戏里的模组目录）：
   ```bash
   git rev-parse HEAD                              # 下面那行应当与它完全相同
   grep -ao "2\.[0-9]\.[0-9]+[0-9a-f]\{40\}" "$MOD/EliteEnemies.dll"
   sha256sum bin/Release/EliteEnemies.dll "$MOD/EliteEnemies.dll"   # 两行哈希应当相同
   ```
   内嵌 SHA ≠ HEAD = 构建之后又提交过，回第 5 步重来。
   顺带 `git status` 也看一眼——**应当是干净的**；脏了说明还有生成物没进提交。

   > ⚠ **发版之后再补文档提交，不要重建。** 那份 DLL 对应的就是打标签的那个提交；
   > 发版后仓库往前走是正常的，重建反而会让 DLL 内嵌的 SHA 不再是标签指的那个。
   > （实测踩过：发版后又提交了一次生成物，一时没想清就重建了。）

7. **打标签**（annotated，说明里写清这次发布的内容与**内嵌 SHA**——
   它等于提交，是"哪份代码是这一版"唯一稳的凭据）：
   ```bash
   git tag -a v2.1.0 -F - <<'EOF'
   v2.1.0
   本次发布内容：……
   发版内嵌 SHA：2.1.0+<第 6 步核对过的那个 SHA>
   EOF
   ```
   打过之后 `git describe --tags` 返回的就是版本号。**核对它指对了没有时要小心**——
   annotated tag 会多一层对象：

   ```bash
   git rev-parse v2.1.0             # ✗ 这是**标签对象**自己的哈希，不是提交
   git rev-parse 'v2.1.0^{commit}'  # ✓ 这才是它指向的提交，应与第 6 步的 SHA 相同
   ```

   > 这个坑实测踩过：拿 `git rev-parse v2.1.0` 去比 DLL 内嵌的 SHA，两个长哈希不一样，
   > 差点以为标签打错了。标签本身没问题，是查法少了 `^{commit}`。

8. **推送**（对外可见，动手前确认）：`git push` + `git push origin v2.1.0`。

9. **上传后确认工坊标签还在** ⚠ **这一条是踩出来的**：上传器会把条目的 tags
   **整个替换**——**留空就等于清空**。曾经因为没填，写好的分类标签全被覆盖，
   模组一度不属于任何类别。

   本模组在工坊页应勾选：**Gameplay** / **Companion & NPC** / **Loot & Economy**
   （可选：Visual Enhancements）。

   不用打开 Steam 就能自查：
   ```bash
   curl -s -X POST "https://api.steampowered.com/ISteamRemoteStorage/GetPublishedFileDetails/v1/" \
        -d "itemcount=1&publishedfileids[0]=3602009885"
   ```
   看返回里的 `tags`——除了 Steam 自动加的 `Mod`，还应当有上面那几个。
   （2026-09-19 查过：当时只剩 `Mod`，其余全没了。）

   > 该游戏工坊开放的标签共 14 个（`steamcommunity.com/app/3167020/workshop/`
   > 左侧筛选栏是权威列表）：
   > Weapon / Equipment & Gear / Loot & Economy / Quality of Life / Cheats & Exploits /
   > Visual Enhancements / Sound / Quest & Progression / Companion & NPC / Collectibles /
   > Gameplay / Multiplayer & Co-op / Utility / Medical & Survival

> **本工程不对外公布文件哈希**（2026-09-18 定的）：
> 工坊页不贴 VirusTotal 检测报告链接，改为请玩家自行检测
> （`workshop\description\*.md` 的「安全性说明」一节）。
> 因此发版时**不需要**重扫、也**不需要**留档发布产物——
> 想自查的话，上传前拿游戏模组目录里那份最新 DLL 扫一遍即可。
>
> ⚠ 顺带一个反直觉的实测：**同一个提交在不同目录构建，DLL 字节不同**
> （确定性构建把源码路径算进了 MVID；同是 `2.1.0+135cde8…`，一处 `974cbdbd…`、
> 另一处 `2c08afa3…`）。所以"重建一份来比对哈希"这种做法不成立——
> 但既然不公布哈希，也就无所谓了。要留记录就留**内嵌 SHA**（它只取决于提交）。

**要上传给工坊的是整个模组目录**（DLL / `info.ini` / `preview.png` / `Localization\` /
`libs\0Harmony.dll`），不是单个 DLL——`libs\` 那个子目录不能少，理由见 `AGENT.md` §3.3。

---

## 四、历史记录

> 每节里的 BBCode 就是当时贴出去的那一份，**保持原样，不要回改**。

### v2.2.0（已发布）

```
[h1]v2.2.0 更新内容[/h1]

[b]✨ 新增[/b]
- 新词条【小偷】：命中时偷走你背包里的一件物品，击杀它会掉落两件
- 新词条【噬弹】：命中时啃掉你弹匣里的子弹
- 新词条【溃伤】：命中后 10 秒内，你的治疗效果减半
```

### v2.1.0（已发布）

```
[h1]v2.1.0 更新内容[/h1]

[b]✨ 新增[/b]
- 【音乐家】现在会真的吹奏曲子：玩家靠近时会用卡祖笛吹《小星星》《两只老虎》《欢乐颂》等 8 首曲子

[b]⚖️ 平衡[/b]
- 【严寒】重做：命中不再直接冻结，改为叠加「寒冷」，叠满 5 层才冻结；冻结结束后有 10 秒免疫
```

### v2.0.0（已发布）

```
[h1]v2.0.0 更新内容[/h1]

[b]⚙️ 优化[/b]
- 语言切换即时生效
- 掉落系统重做
- 设置界面重整
- 各处系统开销优化，降低游戏占用
```
