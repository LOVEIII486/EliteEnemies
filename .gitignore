# --- 编译产物 ---
# 不锚定根目录：tools\ 下的辅助工具项目也会产生 bin\obj，
# 而 /bin/ 这种写法只匹配根目录，会把那些产物放进提交。
bin/
obj/

# --- Python ---
# tools\ 下的分析脚本会产生字节码缓存，它不是源码，不该入库。
__pycache__/
*.pyc

# --- IDE ---
/.vs/
/.idea/
/*.user
/*.userprefs
/*.sln.DotSettings.user

# --- 本地路径覆盖（每台机器不同，不进版本控制） ---
/Directory.Build.props.user

# --- 备份 / 压缩包 ---
*.7z
*.zip
*.rar
*.bak

# 工坊 BBCode 等生成物（由 tools\MdToBbcode 产生）
/artifacts/

# --- Agent / 开发过程文档（不进版本控制） ---
# 这些是给 AI agent 与本地开发用的工作文档（项目指南、模块审查、重构计划…），
# **不是模组的一部分**，公开仓库里也不需要它们——README 只放给人看的内容。
#
# ⚠ 三个目标**都曾经被跟踪过**：只写进 .gitignore 对已跟踪的文件无效，
#   必须配一次 `git rm --cached`（保留工作区文件）才真正取消跟踪。
/docs/
/AGENT.md
/CLAUDE.md
